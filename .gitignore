# Dependencies
/node_modules

# Environment variables (secrets — never commit these)
.env
.env.*

# Local database file (legacy)
database.sqlite

# OS-generated files
.DS_Store
Thumbs.db

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ECS task definitions (downloaded at deploy time, may contain secrets)
task-definition.json
